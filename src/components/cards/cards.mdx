---
name: Cards
---
import { Playground, PropsTable } from 'docz'
import { BasicCard, InteractiveCard } from '~components/cards'
import { Button } from '@blueprintjs/core'

import '@blueprintjs/core/lib/css/blueprint.css'


# A simple card

<PropsTable of={BasicCard} />

## Basic Usage


<Playground>
  <BasicCard title='A Simple Card'>
    <p>Child content passed through for simple state management.</p>
  </BasicCard>
</Playground>

## Info Cards

<Playground>
  {() => {
    let componentState = false;
    const toggleHandler = () => {
      componentState = !componentState
      console.log(componentState)
    }

    return (
      <div>
        <InteractiveCard isOpen={componentState}>
          <h4>An interactive card</h4>
        </InteractiveCard>

        <Button icon='random' minimal onClick={toggleHandler}>Toggle</Button>
      </div>
    )
  }}

</Playground>
