---
name: DotAtlas Wrapper
menu: Components
---
import { Playground, Props } from 'docz'
import Atlas from '.'
import { request } from '~mixins'
import { renderReactComponent } from '~mixins/utils'

# DotAtlas Wrapper

- Provides a configurable component wrapping dotatlas api
- Can access state variables.

<Playground>
  <div id='atlas'/>
</Playground>


request({ url: 'https://noop-pub.s3.amazonaws.com/opt/dotatlas_tmp.json' })
  .then((points) => (
    renderReactComponent('atlas', Atlas, { dataPoints: points })
  ))
